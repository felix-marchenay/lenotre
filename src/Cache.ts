import { fstat } from "fs";

export class Cache
{
    constructor (private path: string = 'cache/') {}

    get(key: string)
    {
        
    }

    set(key: string, value: string): void
    {

    }
}